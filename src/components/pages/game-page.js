import React from 'react';
import Table from '../table';
import Header from '../header';

const GamePage = () => {
  return (
    <React.Fragment>
      <Header />
      <Table />
    </React.Fragment>
  );
}

export default GamePage;